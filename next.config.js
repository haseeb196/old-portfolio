/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['marketifythemes.net', 'cdn.cnn.com', 'i.tribune.com.pk'],
  },
};

module.exports = nextConfig;
